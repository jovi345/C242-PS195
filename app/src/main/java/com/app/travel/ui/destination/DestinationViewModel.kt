package com.app.travel.ui.destination

import androidx.lifecycle.ViewModel
import com.app.travel.data.repo.UserRepository

class DestinationViewModel (private val userRepository: UserRepository) : ViewModel() {
}